/*

Syntax:

Array.isArray(arr)

trong đó arr là đối tượng cần kiếm tra,
*/

// Ví dụ:

var fruits = ["Banana", "Orange", "Apple", "Mango"];
var isArray = Array.isArray(fruits); // true
Array.isArray([2]);//true
Array.isArray(2);//false